# 鸿蒙端

用 DevEco Studio 打开本目录，运行 `entry` 模块。应用使用 HarmonyOS Stage 模型、ArkTS 和 ArkUI，显示名“鸿蒙智阅”，包名 `com.hakimi.smartread`。

默认 API 地址在：

```text
entry/src/main/ets/common/ApiClient.ets
```

模拟器可先使用 `http://127.0.0.1:8080/api`。真机调试时改成运行后端电脑的局域网 IP。
